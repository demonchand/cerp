<%= semantic_form_for @project do |f|%>
    <%= f.inputs do %>
        <%= f.input :name %>
        <%= f.input :description %>
	<%= f.input :users, :label => false, :as => :check_boxes, :collection => User.all %>
    <% end %>
    <%= f.buttons %>
<% end %>
